(jbuild_version 1)

(executables
 ((public_names (camlimages-config))
  (names (ciconfig))
  (preprocess (pps (ppx_meta_conv.ppx)))
  (libraries ( configurator ppx_meta_conv.ocaml findlib spotlib ))))
	   
;   (preprocess (pps (ppx_meta_conv.ppx ppx_orakuda.ppx)))
;   (libraries (sexplib ppx_meta_conv.sexp ppx_orakuda.runtime spotlib xml-light curl tumblr))))

;  (library
;   ((name configlib)
;    (public_name configlib)
;    (libraries ( configurator ))x
;  ))
;  ;  (preprocess (pps (ppx_test.ppx)))
;  ;   (libraries ( unix str ppx_test.runtime ))))
;  
;  
